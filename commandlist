#command line ssh (virtual servermachines) : 
* ping 'ip-adres host' of 'ip van gateway' of 'ip van dns' meegeven als argument aan ping
* ip a (ip address)
* ip r (ip route)
* cat /etc/resolv.conf (dns vbox en gateway vbox)
* sudo systemctl status httpd.service
* yum provides *bin/dig
* sudo journalctl -u httpd.service
* curl -k 'url webpagina'
* nslookup (dns-server request sturen)


#git commands : 

* git init repository-url
* git clone repository-url
* git add .
* git commit -m "message commit"
* git remote add origin repository-url
* git push -u origin master
* git pull



